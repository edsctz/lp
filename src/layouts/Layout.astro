---
interface Props {
  title?: string;
  description?: string;
}

const { 
  title = "Century 21 Alpha | Imobiliária em Alphaville", 
  description = "Century 21 Alpha, sua imobiliária especializada em Alphaville. Compra, venda, aluguel e avaliação de imóveis em Alphaville e região."
} = Astro.props;
---

<!doctype html>
<html lang="pt-BR">
  <head>
    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-NKZQXB9J');</script>
    <!-- End Google Tag Manager -->
    
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    
    <!-- SEO Tags -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="pt_BR" />
    <meta name="twitter:card" content="summary_large_image" />
    
    <!-- Preload fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="antialiased text-c21-black bg-white min-h-screen">
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-NKZQXB9J"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->
    
    <slot />
    
    <!-- Simple analytics script (optional) -->
    <script>
      // Minimal analytics script
      document.addEventListener('DOMContentLoaded', () => {
        const links = document.querySelectorAll('a');
        links.forEach(link => {
          link.addEventListener('click', (e) => {
            const href = link.getAttribute('href');
            const text = link.textContent || link.innerText;
            // You can implement a simple analytics capture here if needed
            // but keeping it minimal for PageSpeed score
          });
        });
      });
    </script>
    
    <!-- WhatsApp link tracking for GTM -->
    <script>
      // Add dataLayer type declaration for TypeScript
      interface Window {
        dataLayer: any[];
      }
      
      document.addEventListener('DOMContentLoaded', () => {
        // Select all WhatsApp links
        const whatsappLinks = document.querySelectorAll('a[href^="https://wa.me"]');
        
        // Add click event listeners to WhatsApp links
        whatsappLinks.forEach(link => {
          link.addEventListener('click', (e) => {
            // Get link URL and extract service type from text parameter
            const href = link.getAttribute('href') || '';
            let serviceType = 'general';
            
            // Extract service type from URL if available
            const textMatch = href.match(/text=.*?(?:falar|sobre)%20([\w]+)/i);
            if (textMatch && textMatch[1]) {
              serviceType = textMatch[1].toLowerCase();
            }
            
            // Get link location (component/section)
            let linkLocation = 'unknown';
            
            // Determine link location based on closest parent section or component
            const parentSection = link.closest('section[id]');
            if (parentSection && parentSection.id) {
              linkLocation = parentSection.id;
            } else if (link.closest('footer')) {
              linkLocation = 'footer';
            } else if (link.closest('.fixed')) {
              linkLocation = 'floating-button';
            }
            
            // Push to dataLayer
            // @ts-ignore - Ignore TypeScript errors for dataLayer
            window.dataLayer = window.dataLayer || [];
            // @ts-ignore - Ignore TypeScript errors for dataLayer
            window.dataLayer.push({
              'event': 'whatsapp_click',
              'whatsapp_service': serviceType,
              'whatsapp_location': linkLocation,
              'whatsapp_url': href
            });
            
            console.log('WhatsApp click tracked:', {
              'event': 'whatsapp_click',
              'whatsapp_service': serviceType,
              'whatsapp_location': linkLocation,
              'whatsapp_url': href
            });
          });
        });
      });
    </script>
    
    <!-- UTM Tracking Script -->
    <script src="/src/utils/utmTracker.js"></script>
    
    <!-- UTM Appender for Outbound Links -->
    <script src="/src/utils/utmAppender.js"></script>
  </body>
</html>

<style is:global>
  :root {
    scroll-behavior: smooth;
  }
  
  body {
    font-family: 'Inter', system-ui, sans-serif;
    line-height: 1.5;
  }
  
  h1, h2, h3, h4, h5, h6 {
    line-height: 1.2;
  }
  
  .container {
    width: 100%;
    max-width: 1280px;
    margin-left: auto;
    margin-right: auto;
    padding-left: 1rem;
    padding-right: 1rem;
  }
  
  @media (min-width: 640px) {
    .container {
      padding-left: 1.5rem;
      padding-right: 1.5rem;
    }
  }
  
  @media (min-width: 1024px) {
    .container {
      padding-left: 2rem;
      padding-right: 2rem;
    }
  }
  
  /* Animation classes */
  .animate-on-scroll {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.5s ease-out, transform 0.5s ease-out;
  }
  
  .animate-on-scroll.is-visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>
